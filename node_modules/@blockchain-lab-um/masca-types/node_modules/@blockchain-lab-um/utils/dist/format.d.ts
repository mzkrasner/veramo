import { type CodecName } from './multicodecs.js';
export declare const encodePublicKey: (pubKeyBytes: Uint8Array, multicodec: CodecName) => string;
export declare const decodePublicKey: (publicKey: string) => {
    pubKeyBytes: Uint8Array;
    code: number;
};
export declare function uint8ArrayToHex(arr: Uint8Array): string;
export declare function hexToUint8Array(str: string): Uint8Array;
export declare function getCompressedPublicKey(publicKey: string): string;
