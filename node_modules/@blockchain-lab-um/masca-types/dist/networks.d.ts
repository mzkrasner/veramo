interface NetworkParams {
    chainId: string;
    rpcUrls: string[];
    chainName: string;
    nativeCurrency: {
        name: string;
        symbol: string;
        decimals: number;
    };
    blockExplorerUrls: string[];
}
declare const chainIdNetworkParamsMapping: Record<string, NetworkParams>;

export { NetworkParams, chainIdNetworkParamsMapping };
