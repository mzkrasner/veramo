import { CredentialStatus, CredentialSchema } from '@0xpolygonid/js-sdk';
import { VerifiableCredential, W3CVerifiableCredential } from '@veramo/core';

interface W3CCredential {
    id: string;
    '@context': string[];
    type: string[];
    expirationDate?: string;
    issuanceDate?: string;
    credentialSubject: Record<string, any>;
    credentialStatus: CredentialStatus;
    issuer: string;
    credentialSchema: CredentialSchema;
    proof?: any;
}
declare const isVerifiableCredential: (input: any) => input is VerifiableCredential;
declare const isW3CVerifiableCredential: (input: any) => input is W3CVerifiableCredential;
declare const isW3CCredential: (input: any) => input is W3CCredential;

export { isVerifiableCredential, isW3CCredential, isW3CVerifiableCredential };
