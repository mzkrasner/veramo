import { VerifiableCredential } from '@veramo/core';

interface QueryCredentialsRequestResult {
    data: VerifiableCredential;
    metadata: {
        id: string;
        store?: string[];
    };
}
interface SaveCredentialRequestResult {
    id: string;
    store: string[];
}

export { QueryCredentialsRequestResult, SaveCredentialRequestResult };
